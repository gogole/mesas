module.exports = 
{
	db:
	{
		host:DB_HOST || "localhost",
		puerto: 3600,
		user: DB_PASS || 'root',
		password: DB_PASS || 'pass',
		database:'Materias'
	},
	server:
	{
		puerto: process.env.PORT || 8000
	}
}